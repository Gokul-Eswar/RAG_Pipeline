apiVersion: v1
kind: Service
metadata:
  name: big-data-rag-api
  labels:
    app: big-data-rag-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: big-data-rag-api
